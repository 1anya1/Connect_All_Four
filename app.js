// alert('were on!')

/*------------Constants------------*/

/*------------Variables------------*/
let board, turn, winner;
/*------------Reference------------*/

/*------------Event Listners-----------*/

/*------------Functions-----------*/
function init(){
    board = [
        [0,0,0,0,0,0], //column1 (index0)
        [0,0,0,0,0,0], //column2 (index0)
        [0,0,0,0,0,0], //column3 (index0)
        [0,0,0,0,0,0], //colum4 (index0)
        [0,0,0,0,0,0], //column5 (index0)
        [0,0,0,0,0,0], //column6 (index0)
        [0,0,0,0,0,0], //column (index0)

    ];
    // calling on the board variable - each array in here represents each item in the board. 
    // here we will create an array for each column
}